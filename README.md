# Lenguajes_BD_Proyecto

Se añadió un proyecto Java Maven básico en este repositorio.

Instrucciones rápidas para compilar y ejecutar:

```bash
mvn package
java -cp target/lenguajes-bd-proyecto-1.0-SNAPSHOT.jar com.example.App
```

